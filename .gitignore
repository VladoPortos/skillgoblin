# Dependencies
node_modules
.pnp
.pnp.js

# Nuxt build output
.nuxt
.nitro
.output
dist

# Database files
*.sqlite
*.sqlite-journal

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS specific
.DS_Store
Thumbs.db

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE / editors
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.mp4
*.srt
*.zip
*.html
course.json

# Claude Code local state
.claude

# Test artifacts
frontend/test-results
frontend/playwright-report
frontend/tests/e2e/.tmp-data

# Local manual-test sandbox (course content + scratch DB; never tracked)
manual-test/

# AI-assistant working notes (handover.md, planning records, design drafts).
# These were primarily produced for cross-session context inside Claude Code
# and aren't user-facing documentation — README.md / SECURITY.md / LICENSE
# are. Keeping them out of git so the repo stays focused on code + the
# docs operators actually need.
notes/
# Course.json files written by the e2e fixture during test runs.
frontend/tests/fixtures/content/**/course.json
